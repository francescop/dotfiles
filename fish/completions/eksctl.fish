Generates shell completion scripts

Usage: eksctl completion [flags]

Commands:
  eksctl completion bash                    Generates bash completion scripts
  eksctl completion zsh                     Generates zsh completion scripts

Common flags:
  -C, --color string   toggle colorized logs (valid options: true, false, fabulous) (default "true")
  -h, --help           help for this command
  -v, --verbose int    set log level, use 0 to silence, 4 for debugging and 5 for debugging with AWS debug logging (default 3)

Use 'eksctl completion [command] --help' for more information about a command.

